CREATE TABLE CLIENT (ID BIGINT NOT NULL, ADDRESS VARCHAR(512) UNIQUE, CPF VARCHAR(11) UNIQUE, EMAIL VARCHAR(60) UNIQUE, MARITALSTATUS VARCHAR(10) UNIQUE, NAME VARCHAR(256) UNIQUE, PRIMARY KEY (ID))
CREATE TABLE PHONE (ID BIGINT NOT NULL, PHONENUMBER VARCHAR(20), CLIENT_ID BIGINT, PRIMARY KEY (ID))
ALTER TABLE PHONE ADD CONSTRAINT FK_PHONE_CLIENT_ID FOREIGN KEY (CLIENT_ID) REFERENCES CLIENT (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
